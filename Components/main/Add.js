import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const Add = () => {
    return (
        <View>
            <Text>Add</Text>
        </View>
    );
}

const styles = StyleSheet.create({})

export default Add;
